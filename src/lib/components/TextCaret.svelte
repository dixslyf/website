<div class="text-caret-container"><div class="text-caret"></div></div>

<style>
  .text-caret-container {
    position: relative;
    display: inline;
  }

  .text-caret {
    position: absolute;
    display: inline-block;
    vertical-align: middle;

    margin-left: 2px;
    bottom: 0.08em;

    height: var(--height, 1.2em);
    width: var(--width, 0.1em);

    border-radius: var(--radius, 8px);

    background-color: var(--color, var(--ctp-macchiato-text));
  }
</style>
